# Stage18 P100 Semantics Smoke

Date: 2026-06-23

Status: `runtime_sanity / exploratory_not_validation`.

## Purpose

This smoke run checked whether the current remote execution lane can still run
small TCLB cases on the Tesla P100 GPUs while the source rebuild branch proceeds.

It is not a contact-angle validation and not a solver-physics fix.

## Remote Run

Run root:

```text
/mnt/usb1t/RUNS/runs/stage18_semantics_smoke_zaxis_20260623
```

Script:

```text
/home/yuan/run_stage18_semantics_smoke_remote.sh
repo/scripts/stage18/run_stage18_semantics_smoke_remote.sh
```

Local evidence:

```text
artifacts/stage18_semantics_smoke_20260623/
```

Cases:

```text
plane_theta090_100
cylinder_theta090_old_axis_100
cylinder_theta090_z_axis_100
```

## Result

Remote `status.txt`:

```text
plane_rc=0
cylinder_old_axis_rc=0
cylinder_z_axis_rc=0
```

The return code alone is insufficient because TCLB failcheck can stop after
detecting NaN while the wrapper still returns 0. The log content is the authority.

### Flat wall

```text
AnalyticSolidType=1
AnalyticSolidAxis=1
iterations=100
VTK frames=0 and 100
NaN marker in log: none observed
```

Verdict:

```text
flat runtime chain OK
```

### Cylinder, old stage9 axis

```text
AnalyticSolidType=2
AnalyticSolidAxis=0
source template=cases/diagnostics/stage9_analytic_wetting_20260614/cylinder_theta090/case.xml
```

Log evidence:

```text
Checking P discovered NaN
NaN value discovered
Stopping due to Nan value
```

Verdict:

```text
old stage9 cylinder template remains dangerous/deprecated
```

This reproduces the historical axis/geometry risk. It should not be used as the
current cylinder gate.

### Cylinder, corrected z-axis route

```text
AnalyticSolidType=2
AnalyticSolidAxis=2
source template generated by /home/yuan/gen_cyl_compact.py
iterations=100
VTK frames=0 and 100
NaN marker in log: none observed
```

Verdict:

```text
z-axis cylinder runtime chain OK for a 100-step smoke
```

This only proves that the corrected z-axis execution path launches and survives
100 steps. It does not prove contact-angle accuracy.

## Space Note

This smoke generated raw VTI files on the remote server:

```text
/mnt/usb1t/RUNS/runs/stage18_semantics_smoke_20260623 -> about 583M
/mnt/usb1t/RUNS/runs/stage18_semantics_smoke_zaxis_20260623 -> about 2.2G
```

The local repo keeps only logs and XML evidence. The remote raw VTI files can be
deleted after user approval because this smoke is runtime evidence, not
morphology evidence.

## Consequence

Future cylinder tests must use the z-axis route:

```text
AnalyticSolidAxis=2
Wall zone name=AnalyticCylinder
generator family=scripts/stage12/stage12_validation_run.py or scripts/stage13/gen_cyl_compact.py
```

Do not use the historical stage9 axis=0 cylinder templates as validation inputs.
