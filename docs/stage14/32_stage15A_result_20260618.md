# Stage 15A Result — M=0.1 Equilibrium @12000 (t150 confound resolved) — 2026-06-18

This resolves the time-length confound flagged in doc 29. It is the gate that
decides whether the t150 obtuse drift is a mobility effect or a static
long-time effect. Diagnostic-only; only case-parameter variation.

```text
status_label: exploratory_not_validation
claim_limit:  diagnostic equilibrium check only
```

## 0. Headline — the confound is resolved, and the conclusion changes strategy

```text
M=0.1 @ 12000 steps ALSO drifts on t150: theta_end = 152.25 deg, drift +0.204 deg/1k.
The t150 obtuse drift is present at ALL mobilities tested (0.1/0.2/0.3); its rate
is only weakly mobility-dependent. It is NOT a mobility-threshold effect.
```

This means:
- doc 25's "M=0.1 holds t150 (150.09 deg)" was a **time-length artifact** — that
  run was 1500 steps; at +0.2 deg/1k a drift moves only ~0.3 deg in 1500 steps
  (noise-level) but ~2.5 deg in 12000 steps.
- Dropping mobility (0.15, etc.) will NOT fix the t150 drift. **Stop the
  mobility-descent loop.**
- DynamicCL (R_θ) will NOT fix it either — R_θ≈0 at equilibrium by construction.
- The t150 drift is a SEPARATE static-obtuse / phase-field problem, tracked in
  parallel, NOT blocking the changed-angle-dynamics work.

## 1. Result table (M=0.1, equilibrium, 12000 steps)

| case | target | theta_end | err | drift/1k (last 4k) | RMS_end | mass drift % | footprint |
|---|---|---|---|---|---|---|---|
| t30 | 30 | 30.153 | +0.153 | −0.0001 | 0.064 | −0.36 | 32.02→32.31 |
| t90 | 90 | 90.320 | +0.320 | +0.030 | 0.024 | +0.01 | stable |
| t150 | 150 | **152.254** | **+2.254** | **+0.204** | 0.117 | +0.10 | 10.82→9.49 |

## 2. The three-mobility comparison (the decisive evidence)

| M | t150 theta_end | t150 err | t150 drift/1k | t150 footprint change |
|---|---|---|---|---|
| 0.1 (this) | 152.25 | +2.25 | +0.204 | 10.82→9.49 |
| 0.2 (14C-2b) | 152.68 | +2.68 | +0.228 | 10.82→9.49 |
| 0.3 (14C-2) | 152.73 | +2.73 | +0.243 | (n/a this run) |

```text
M 0.1 -> 0.3 (3x mobility) changed t150 drift by only +0.039 deg/1k.
The drift is NOT a mobility threshold; it is present and ~constant across M.
```

## 3. What this means for the plan (revised branching)

```text
t150 obtuse drift:
  - present at all M, weakly M-dependent
  - NOT fixable by lowering M
  - NOT fixable by DynamicCL (R_theta ~ 0 at equilibrium)
  => track as a SEPARATE static-pin / phase-field diagnostic (Stage 15D-par).
     candidates: obtuse-cap geometric pin error amplified over long time;
     circle-arc-fit artefact at obtuse angles; compact-ghost static residual.
     Do NOT let it block the changed-angle dynamics work.

Working mobility:
  - all of 0.1/0.2/0.3 preserve t30/t90 equilibrium (<0.5 deg) at 12000 steps
  - the t150 drift is non-selective across M, so M can be chosen on speed
  - 14C-1 showed decoupled speed rises ~2.5x from M0.1 to M0.4; M=0.3 is the
    speed sweet spot, and its t30/t90 equilibrium is fine
  - RECOMMEND working mobility M=0.3 (speed) for the DynamicCL work, accepting
    the t150 drift as a separate known issue
```

## 4. Revised stage ordering

```text
15A  M=0.1 @12000 equilibrium         DONE (this doc) -> confound resolved
15B-pre safety cleanup                 disable WallGradMode=2 in calcGradPhi;
                                       always-consume ghost in stencil
15B  DynamicCLMode=1 shadow            R_theta = cos_eq - cos_app; F_total slot
15C  DynamicCLMode=2 small-coeff write compensate ~6x tangential dilution
15D-par (parallel) t150 static drift   own diagnostic, NOT blocking 15B/15C
```

The DynamicCL path (15B/15C) proceeds at the working mobility M=0.3, because:
- it targets CHANGED-angle dynamics (decoupled), not static equilibrium
- the compact ghost is healthy (14A) and F_surf direction is correct (14C-prime)
- 15A confirms M does not gate the t150 static drift, so M is free to optimize
  for the dynamic speed

## 5. Reproducibility

```text
bash /home/yuan/stage14c_equilibrium_atM.sh 0.1
python3 /home/yuan/stage14c_eq_postprocess.py 0.1   (output in stage14c_post/)
```

## 6. Honest note

doc 29 flagged this confound explicitly and recommended 15A before any further
mobility descent. 15A confirms the confound was real: the "M=0.1 holds t150"
claim was a 1500-step artifact. Recording this so the reasoning chain is
auditable — the prior mobility-descent recommendation (doc 28) is now withdrawn.
